# Bloqueia acesso direto aos arquivos JSON desta pasta (Apache 2.4+)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Fallback para Apache 2.2
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
